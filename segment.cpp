//
// Created by loona on 2022-01-09.
//

#include "segment.h"

segment::segment(vector<float> start, vector<float> end, bool selected) {
    // set the shape and then its width and then its start and end points
    this->shape
    if (selected) segment::recolour();
}

void segment::recolour() {}

segment::~segment() = default;